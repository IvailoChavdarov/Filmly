@model IEnumerable<Titles>
    <H1>Favourites</H1>
<table>
    @foreach (var title in Model)
    {
        <tr>
            <td>@title.FullTitle</td>
            <td><img src="@title.Image" alt="Alternate Text" /></td>
        </tr>
    }

</table>
